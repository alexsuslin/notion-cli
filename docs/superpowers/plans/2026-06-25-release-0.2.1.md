# Release 0.2.1 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Prepare the repository for a `0.2.1` patch release from the existing unreleased changes.

**Architecture:** This is a metadata-only release preparation. The package version stays synchronized between `pyproject.toml` and `src/notion_cli/__init__.py`, and `CHANGELOG.md` remains the source of release notes.

**Tech Stack:** Python packaging with Hatchling, pytest, Ruff, mypy, GitHub Releases.

---

### Task 1: Promote Release Metadata

**Files:**
- Modify: `pyproject.toml`
- Modify: `src/notion_cli/__init__.py`
- Modify: `CHANGELOG.md`

- [ ] **Step 1: Confirm release scope**

Run: `gh issue list --limit 50 --state open --json number,title,url`
Expected: `[]`

- [ ] **Step 2: Update package version**

Change `pyproject.toml`:

```toml
version = "0.2.1"
```

Change `src/notion_cli/__init__.py`:

```python
__all__ = ["__version__"]

__version__ = "0.2.1"
```

- [ ] **Step 3: Promote changelog notes**

Change `CHANGELOG.md` so the current unreleased entries move under:

```markdown
## [0.2.1] - 2026-06-25
```

Keep an empty `## [Unreleased]` section above it for future changes.

- [ ] **Step 4: Run required verification**

Run:

```bash
pytest -v
ruff check .
mypy src
```

Expected: all commands pass.

- [ ] **Step 5: Inspect final diff**

Run: `git diff -- pyproject.toml src/notion_cli/__init__.py CHANGELOG.md docs/superpowers/plans/2026-06-25-release-0.2.1.md`
Expected: only release metadata, changelog promotion, and this plan changed.

